services:
  server:
    build:
      context: .
      dockerfile: packages/server/Dockerfile
    image: coinz-server:local
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
